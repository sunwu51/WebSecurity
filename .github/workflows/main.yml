name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo $PATH
        node -v
        npm -v
        python --version
        docker version
        java -version
        go version
        mvn -version
